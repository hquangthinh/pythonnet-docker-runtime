name: build pythonnet image aspnet8

on:
  workflow_run:
    workflows: ["build base image aspnet8"]
    types:
      - completed
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: ./src/dotnet-python/aspnet8_python312_pythonnet/bookworm
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/dotnet-python:8.0-3.12-bookworm-pythonnet
